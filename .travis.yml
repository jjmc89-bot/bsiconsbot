language: python
python:
    - 3.5
    - 3.6
    - 3.7
sudo: false
os: linux
install:
    - pip install -U setuptools
    - python setup.py install
    - pip install pylint
script:
    - pylint bsiconsbot scripts
